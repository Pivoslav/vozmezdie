# Config

Place shared config schemas, default paths, and taxonomy references here.

Expected (later):
- Document roots / input paths
- Ground truth path or pattern
- Model ID, API URL, prompt variant (with/without context)
- Taxonomy file (categories + framing lists + glossary text)
